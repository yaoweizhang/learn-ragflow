# s00 序言 — RAG 全景、RAGFlow 与项目导读

> **章节定位**：本教程的序章。读完能用 5 分钟向同事讲清楚 RAG 是什么、解决什么问题、什么时候不该用、12 章各自承担什么角色。
>
> **章节结构**：5 个 H2：核心定义 / 选型维度 / 本项目与 RAGFlow / 项目导读 / 思考题。**没有 code 文件**——s01 才是第一个写代码的章节。
>
> **scope 注意**：s00 是"心智模型建立"，不是代码教程。所有表格、流程图、思考题答案都内嵌在本文。

---

## 一、RAG 是什么

### 1.1 一句话定义

**RAG（Retrieval-Augmented Generation，检索增强生成）= 检索 + 增强 + 生成**。

核心思想：在 LLM 生成回答之前，先从外部知识库里按问题查相关文档，把找到的内容拼进 prompt，再让 LLM 基于这段上下文作答。

这等于让 LLM 从"闭卷考试"变成"开卷考试"——既能利用模型自己学到的"参数化知识"（固化在数十亿参数权重里），也能随时查阅外部"非参数化知识"（存放在外部知识库里的精准资料）。RAG 把这两类知识**按需结合**——这一结合本身，比 RAG 这个名词里任何单独一个字都更重要。

> 💡 让 LLM "开卷考试"——既能利用自己的参数化记忆，也能随时翻阅外部资料。

### 1.2 离线 / 在线 两条流水线

RAG 系统的工作流可以拆成两阶段：**离线**那条线把语料变成可检索的索引，**在线**那条线把用户问题变成有据可查的回答。

```
┌─────────────────────────┐                ┌─────────────────────────┐
│  离线：索引构建         │                │  在线：问答             │
│                         │                │                         │
│  文档（PDF/DOCX/        │  ┌──────┐      │  用户问题               │
│  HTML/Markdown）        │─▶│ 切块 │      │       │                 │
│       │                 │  └──────┘      │       ▼                 │
│       ▼                 │       │        │  ┌──────────┐           │
│  ┌──────────┐           │       ▼        │  │ 同款     │           │
│  │ 解析     │           │  ┌─────────┐   │  │ Embedding│           │
│  │ (DeepDoc)│           │  │ Embedding│  │  └──────────┘           │
│  └──────────┘           │  └─────────┘   │       │                 │
│       │                 │       │        │       ▼                 │
│       ▼                 │       ▼        │  ┌──────────┐           │
│  ┌──────────┐           │  ┌─────────┐   │  │ 向量召回 │           │
│  │ 结构化   │           │  │ 向量    │   │  │ + BM25   │           │
│  │ (parent) │           │  │ 数据库  │   │  └──────────┘           │
│  └──────────┘           │  └─────────┘   │       │                 │
│                         │                │       ▼                 │
│                         │                │  ┌──────────┐           │
│                         │                │  │ Rerank   │           │
│                         │                │  └──────────┘           │
│                         │                │       │                 │
│                         │                │       ▼                 │
│                         │                │  ┌──────────┐           │
│                         │                │  │ LLM 生成 │──▶ 回答   │
│                         │                │  └──────────┘           │
└─────────────────────────┘                └─────────────────────────┘
```

- **离线阶段**：文档解析（s02）→ 切块（s03）→ Embedding（s04）→ 写向量库（s05）。
- **在线阶段**：问题 Embedding → 双路召回（向量 + BM25，s06）→ Rerank 精排（s07）→ 拼 Prompt + LLM 生成（s08）。

> "离线"是提前完成的预处理；"在线"是用户发起请求后的实时处理。

### 1.3 三代演进

学术界普遍把 RAG 演进分成三代：

```
  2020-2022 初级   naive RAG: 检索 → 生成(单步)
  ────────────────────────────────────────▶
  2023-2024 高级   advanced RAG: 检索前/检索后/检索中(加 rerank/重写/压缩)
  ────────────────────────────────────────▶
  2024-2025 模块化 modular RAG: 流水线 + Agent 路由(本教程的 s09-s10 走这一支)
  ────────────────────────────────────────▶
        │                  │                     │
        ▼                  ▼                     ▼
     s01/s06              s06-s08               s09/s10
```

| | **初级 RAG（Naive）** | **高级 RAG（Advanced）** | **模块化 RAG（Modular）** |
|---|---|---|---|
| **流程** | 离线：`索引`<br>在线：`检索 → 生成` | 离线：`索引`<br>在线：`… → 检索前 → … → 检索后 → …` | 积木式可编排流程 |
| **特点** | 基础线性流程 | 在检索前后增加优化步骤 | 模块化、可组合、可动态调整 |
| **关键技术** | 基础向量检索 | 查询重写、结果重排（Rerank） | 动态路由、查询转换、多路融合 |
| **局限性** | 效果不稳定、难以优化 | 流程相对固定、优化点有限 | 系统复杂性高 |

### 1.4 12 章对照

本教程的章节对应关系：s01-s08 是**初级 RAG** 的最小骨架；s06 + s07 进入**高级 RAG**（混合检索 + Rerank）；s09（Agent）+ s10（GraphRAG）+ s12（部署）属于**模块化 RAG** 的不同切片。

| 章 | 主题 | 在 RAG 全景中的位置 | RAG 链路角色 |
|---|---|---|---|
| s01 | RAG 入门 | 心智模型 | retrieve + augment + generate 最小闭环 |
| s02 | 文档加载 | 离线 | PDF / DOCX 解析为统一 schema |
| s03 | 文本分块 | 离线 | 固定字符 cap + 句界切 |
| s04 | Embedding | 离线 / 在线 | 本地 BGE / OpenAI / Ollama |
| s05 | 向量索引 | 离线 | Chroma 持久化 + metadata 过滤 |
| s06 | 混合检索 | 在线 | BM25 + dense 加权融合 |
| s07 | 重排序 | 在线 | BGE cross-encoder 精排 |
| s08 | Prompt 与生成 | 在线 | 引用 [i] / 拒答 / 角标对齐 |
| s09 | Agent 与工具 | 在线 | 模块化路由 + 工具调用 |
| s10 | GraphRAG | 高级 | 实体关系抽取 + 1 跳查询 |
| s11 | 多模态 | 离线 / 在线 | 表格 / OCR |
| s12 | 部署 | 上线 | FastAPI + Docker |

### 1.5 RAGFlow 在演进里的位置

RAGFlow 是 **Modular RAG 的代表实现**——把 RAG 主干的每一环拆成独立模块，模块之间通过工厂模式 + 调度器组合，可以按 query 类型动态编排路径。本教程的 s02-s12 一一对照 RAGFlow 的 12 个工程模块，详见 §3。

---

## 二、为什么用 RAG：选型维度

### 2.1 RAG vs 长上下文 vs 微调

在实际工程里，**RAG 不是万能药，也不是首选药**。通用准则：能不改模型就别改模型，成本与可控性都更好。具体选型路径通常是 **提示词工程 → 检索增强生成 → 微调**。

- **横轴 = LLM 优化深度**——从左到右，对模型本身的改动越来越深。提示工程和 RAG **完全不动**模型权重，微调则直接修改参数。
- **纵轴 = 上下文优化深度**——从上到下，给模型的输入信息越来越丰富。提示工程只是改写提问；RAG 通过外挂知识库极大地扩充上下文。

```
           成本 ↓        知识新鲜度 ↑        可控性 ↓
          ───────────────────────────────────────────────
  长上下文  │  ★ (随语料线性增长)   │  ★ (只要塞进 prompt)   │  ★ (无外接依赖)        │
          ───────────────────────────────────────────────
  RAG     │  ★★ (检索一次，长期复用) │  ★★ (索引可热拔插)   │  ★★ (引用 + 拒答可控) │
          ───────────────────────────────────────────────
  微调     │  ★★★ (训练 + 推理双成本)│  ★ (改权重才能更新)   │  ★★★ (行为 / 风格可控)│
          ───────────────────────────────────────────────
```

RAG 在中间，把"低成本 + 高新鲜度 + 中等可控性"这个最难占到的格子拿到手。这也是为什么 RAG 是工业界的事实标准。

### 2.2 经验法则 + 风险分级

RAG 并非万能。按风险等级给出典型用例与 RAG 适用性：

| 风险等级 | 典型案例 | RAG 适用性 |
|---|---|---|
| **低风险** | 翻译、语法检查 | 高可靠性 |
| **中风险** | 合同起草、法律咨询 | 需结合人工审核 |
| **高风险** | 证据分析、签证决策 | 需严格质量控制机制 |

在 **高风险** 场景里，RAG 自身不足以担责——它的输出必须配合人工复核、规则校验或领域专家评审。

### 2.3 适合谁、不适合谁

- **适合**：客服问答、企业知识库、法规检索、文档型 RAG、检索质量敏感的中等规模场景。
- **不适合**：纯创作 / 闲聊（没有"事实可查"的需求）、高频但语料极小（直接拼 prompt 更省事）、需要"行为 / 风格定制"（应该走微调而非 RAG）。

---

## 三、本项目与 RAGFlow

### 3.1 RAGFlow 是什么

[RAGFlow](https://github.com/infiniflow/ragflow) 是 GitHub 上 star 数最高的开源 RAG 引擎之一。它把 RAG 全链路的每一环——文档解析、文本切块、embedding、向量索引、混合检索、Rerank、Prompt 模板、Agent 编排、图谱抽取、多模态解析、部署——都做了工程化，写成可独立部署、可独立替换的模块。

RAGFlow 的工程设计原则：**每一环独立可替换；每环可单独生产部署；新接一个 provider（embedding / vector DB / LLM）只需要写一个类 + 在 factory 注册一行**。

### 3.2 为什么选 RAGFlow 做工业参考

- **工程化程度高**——每个模块都有完整接口抽象、生产级实现、单元测试
- **可读性强**——RAGFlow 的代码组织清晰，比 LangChain / LlamaIndex 更适合"做深读"
- **生产实战**——RAGFlow 自带 docker-compose 部署方案，处理过高并发 / 大语料 / 多租户
- **覆盖本教程的每一环**——本教程的 12 章内容 RAGFlow 都实现了工业版

LangChain / LlamaIndex / Dify 等其他框架各有特点（LangChain 抽象层多、LlamaIndex 偏数据框架、Dify 偏可视化），本教程选 RAGFlow 作为**唯一**工业参考，让读者把"30 行玩具 vs 1000 行生产"的对比关系看透。

### 3.3 本教程如何使用 RAGFlow

每章（s01-s12）的末尾都有一节 `## RAGFlow 实现`，摘录 RAGFlow 在该层的工程化做法：

- **s01 散文式**：s01 跑的是 RAG 全栈最小骨架，RAGFlow 把这条主干拆成 12 个独立模块（每章对一块）。s01 不重复列出 12 章地图（见 §1.4）。
- **s02-s12 模块化**：每个 RAGFlow 实现小节含 3 部分——RAGFlow 模块定位（哪个文件/目录）、设计取舍（为什么这样做）、`docs/reference/ragflow-notes/<topic>.md` 链接（5-15 行"为什么这样写"的源码分析）。

**默认阅读路径**：s01-s12 主线 + 工业参考 [`docs/reference/ragflow-notes/`](docs/reference/ragflow-notes/) 摘录，**并行推进**。本教程是 RAGFlow 学习项目，ragflow-notes 不是"补充"——是教学路径的另一半。

---

## 四、项目导读

### 4.1 教程目标

**用 12 个 30-80 行的 MVP 跑通 RAG 全链路，同时对照 RAGFlow 工业级源码，看懂每一行为什么这样写**。

不是让你"看完理论"，而是让你**写完 12 个 MVP 玩具**之后，能讲清楚 RAG 系统每一段在做什么、为什么这样做、生产里又会变成什么样。

### 4.2 阅读路径

**快路径（2-3 小时）**：s01 → s06 → s08 → s12。重点是**全链路打通**。

**完整路径（10-12 小时）**：s01 → s02 → ... → s12，按章跑。每章 30-60 分钟：跑 `code_NN_<topic>.py` + 改 `code_NN_<topic>.py` 看变化 + 读对应 `docs/reference/ragflow-notes/<topic>.md`。

**深读路径（推荐）**：完整路径 + 每章末 `## RAGFlow 实现` + `docs/reference/ragflow-notes/<topic>.md` 的源码摘录与"为什么这样写"分析。

### 4.3 项目结构

```
learn-ragflow/
├── README.md                     # 本项目总览（中文）
├── README.en.md                  # 英文 mirror
├── samples/                      # 共享样本：server_whitepaper.pdf + disclosure.docx
├── docs/
│   └── reference/
│       └── ragflow-notes/        # RAGFlow 工业级源码摘录（每章一个 .md）
└── s0?_*/                        # 12 章各自目录
    ├── README.md                 # 章节入口：导航 + 每 code 文件独占 H2
    └── code_NN_<topic>.py        # 30-80 行可独立运行
```

每章 `README.md` 内部统一 H2 骨架（按 code 文件数自适应）：

- **一、章节介绍**——本章要解决什么问题、为什么这么设计、对应 RAG 全景哪一节。
- **二 / 三 / 四、<code 主题>：`code_NN_xxx.py`**——每个 code 文件独占 H2，下含 `这是什么 / 跑起来 / 它做对了什么 / 它做错了什么 / 思考题` 5 个 H3。
- **+1、其他 / 整体设计取舍**——跨 code 文件的设计决策、schema 形状、troubleshooting。
- **RAGFlow 实现**——本层在 RAGFlow 哪个文件、为什么这样做、源码摘录链接。
- **+1、其他 / 选型与思考题**——主流方案速览 + 选型速记 + 本章思考题。
- **思考题答案**（最后一个 H2）。

1 code 文件的章（s07 / s08 / s12）：一/二/三/四/五 + 思考题答案。2 code 文件的章：再加一个 H2。3 code 文件的章（s00 / s01 / s03）：共 7 个 H2 段。

### 4.4 工业参考 ragflow-notes 怎么用

[`docs/reference/ragflow-notes/`](docs/reference/ragflow-notes/) 目录下有 12 个 .md 文件，对应 s02-s12 各章。每个文件含 5-15 行 RAGFlow 真实源码摘录 + "为什么这样写"的注释分析。

**使用建议**：
1. 读完一章的 MVP 后，**立刻读**对应 ragflow-notes 文件（"30 行玩具 vs 1000 行生产"的差距一目了然）
2. 重点看"为什么这样写"分析——RAGFlow 的设计取舍比代码本身更有学习价值
3. 跟着 RAGFlow 源码翻到 `infiniflow/ragflow` GitHub 仓库的对应文件，看完整实现

RAGFlow 持续演化，摘录可能与最新版不一致——重点学**设计意图**，不是逐行背诵。

---

## 五、选型速记

### 5.1 选型速记

- **教学 / 玩具 / 文档 < 100 段** → 朴素 RAG（s01 MVP），零依赖、能跑通
- **中小规模生产 / 文档 100-100k 段** → 初级 RAG + s04 BGE + s06 BM25+dense 融合 + s07 rerank + s08 prompt 模板
- **检索质量敏感 / 命中率优先** → 高级 RAG 叠加：HyDE 查询重写 + 多路召回 + cross-encoder
- **复杂多源 / 跨系统 / 多跳推理** → 模块化 RAG：s09 Agent + s10 GraphRAG + 工具调用
- **"行为 / 风格定制"优先** → 微调，不要硬套 RAG
- **想学工业实现** → RAGFlow 本教程配套 [`docs/reference/ragflow-notes/`](docs/reference/ragflow-notes/) 必读

---

## 思考题

1. **为什么"参数化 + 非参数化按需结合"比单独说"检索 / 增强 / 生成"中任一词都重要？**
2. **RAG vs 长上下文 vs 微调，三者的成本 / 新鲜度 / 可控性取舍矩阵里，RAG 卡在哪个格子？**
3. **三代 RAG（Naive / Advanced / Modular）的"分水岭"是"加了 Rerank"还是"加了 Agent 路由"？为什么？**
4. **本教程 12 章中，s06（混合检索）和 s07（重排序）属于哪一代？s09（Agent）和 s10（GraphRAG）呢？**

（答案见文末"思考题答案"）

---

## 思考题答案

### Q1. 为什么"参数化 + 非参数化按需结合"比单独说"检索 / 增强 / 生成"中任一词都重要？

任何一项单独都不能解决 LLM 的真实问题：

- 单说"检索"——LLM 拿到一堆 chunk 也不会自动答对（不会"增强"生成）
- 单说"增强"——没有外部检索来源，"增强"的是模型自己的参数化记忆，跟闭卷答题没区别
- 单说"生成"——这是所有 LLM 都能做的，谈不上 RAG 的价值

只有三者按需结合——按问题类型决定"主要靠参数化记忆"还是"主要靠外部检索"、再让 LLM 把两类知识综合成答案——RAG 才成立。这跟"开卷考试"是一个比喻：检索 = 翻书、增强 = 把书摊在桌上、生成 = 写出答案。三个动作缺一不可。

### Q2. RAG vs 长上下文 vs 微调，三者的取舍矩阵里 RAG 卡在哪个格子？

RAG 卡在"低成本 + 高新鲜度 + 中等可控性"这个格子：
- **成本**：检索一次建索引，长期复用——比长上下文（每次都塞全语料）便宜得多
- **新鲜度**：索引可热拔插——比微调（改权重才能更新）灵活得多
- **可控性**：引用 [i] + 拒答 prompt 模板——介于长上下文（无外接依赖）和微调（行为完全可控）之间

这个三角的"左下角"是工业界最常卡的位置：高频 + 大语料 + 重复问题几乎总是 RAG 便宜。

### Q3. 三代 RAG 的"分水岭"是"加了 Rerank"还是"加了 Agent 路由"？为什么？

**加了 Agent 路由**才是真正的分水岭。

- 初级 → 高级：Rerank / 查询重写 / HyDE / 多路召回 都是**流程内的优化**——还是一条直线，只是直线上每个节点更精细。流程结构没变。
- 高级 → 模块化：Agent 路由让流程从**直线变成图**——同一个 query 可能走"向量召回 → LLM"、也可能走"BM25 → 重写 → 向量召回 → LLM"、还可能走"工具调用 → 多跳 → 融合"。流程不再固定，按 query 动态选路。这是从"优化既有流程"到"动态编排流程"的范式跃迁。

所以分水岭在 Agent 路由，Rerank 只是高级 RAG 内部的关键技术之一（s07 详谈），模块化 RAG 的关键技术在 s09（Agent）+ s10（GraphRAG）。

### Q4. 本教程 12 章中，s06（混合检索）和 s07（重排序）属于哪一代？s09（Agent）和 s10（GraphRAG）呢？

- s06（混合检索）——基础链路是初级 RAG；但 s06 加上 α-weighted BM25+dense 融合属于**高级 RAG**（在检索环节做了优化）
- s07（重排序）——典型**高级 RAG** 特征（在检索后做精排）
- s09（Agent）——典型**模块化 RAG** 特征（动态路由 + 工具调用）
- s10（GraphRAG）——**高级 RAG 的延伸**（多跳检索），但实现上走模块化管线（按 query 类型走不同检索路径），所以也归到模块化 RAG