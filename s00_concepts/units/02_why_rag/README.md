# s00 / Unit 02 — 为什么用 RAG

> 由浅入深第 2 步：**用一张"选型二维图 + 风险分级表"讲清楚"RAG 不是万能药、也不是首选药"**。  
> 本章不依赖任何前置 unit；3 个 unit 可任意顺序读。

## 一、为什么用 RAG？

### 1.1 技术选型：Prompt → RAG → 微调

在实际工程里，**RAG 不是万能药，也不是首选药**。通用准则：能不改模型就别改模型，成本与可控性都更好。具体选型路径通常是 **提示词工程 → 检索增强生成 → 微调**。

我们可以从一个二维图来理解这三者的位置：

- **横轴 = LLM 优化深度**——从左到右，对模型本身的改动越来越深。提示工程和 RAG **完全不动**模型权重，微调则直接修改参数。
- **纵轴 = 上下文优化深度**——从下到上，给模型的输入信息越来越丰富。提示工程只是改写提问；RAG 通过外挂知识库极大地扩充上下文。

基于这个二维图，三者的分工就清晰了：

- **先尝试提示工程**——任务简单、模型已有相关知识时，精心设计的 prompt 就够用。
- **再选择 RAG**——当模型缺乏特定或实时知识而答不上来时，用 RAG 给它外挂一份"参考资料"。
- **最后考虑微调**——只有当目标是**改变模型"如何做"**（行为、风格、格式），而不是**"知道什么"**（事实性知识）时，微调才是合适的。例如让模型严格遵循某种独特的输出格式、模仿特定人物的对话风格，或把极其复杂的指令"蒸馏"进权重。

RAG 解决了 LLM 的四类典型局限：

| LLM 的局限 | RAG 的解法 |
|---|---|
| **静态知识**：训练截止后的事 | 实时检索外部知识库，支持动态更新 |
| **幻觉（Hallucination）** | 基于检索内容生成，错误率显著降低 |
| **领域专业性不足** | 引入领域特定知识库（医疗、法律、企业内部） |
| **数据隐私风险** | 本地化部署知识库，敏感数据不出域 |

### 1.2 关键优势

（1）**准确性与可信度的双重提升**

RAG 最核心的价值是突破了模型预训练知识的边界。它不仅能**补充专业领域的知识盲区**，还能通过提供具体参考材料有效**抑制"一本正经胡说八道"的幻觉**。更重要的是 **可溯源性**——每一条回答都能找到对应的原始文档出处，这种"有据可查"的特性在法律、医疗等严肃场景下尤其关键。

（2）**时效性保障**

RAG 解决了 LLM 的**知识时滞**——模型不知道训练截止日期之后发生的事。RAG 允许知识库**独立于模型动态更新**：新政策或新数据一旦入库，立刻就能被检索到。这种能力被社区称为 **"索引热拔插"（Index Hot-swapping）**——就像给机器人换一张存储卡，瞬间切换其世界知识库，无需重新训练模型。

（3）**显著的综合成本效益**

RAG 是一种高性价比的方案。首先，它**避免了高频微调**带来的巨额算力成本；其次，有了外部知识的辅助，我们往往可以用**参数量更小的基础模型**达到类似效果，直接降低推理成本；最后，它减少了"把海量知识强行塞进模型权重"的资源消耗。

（4）**灵活的模块化可扩展性**

RAG 架构天然支持**多源集成**（PDF、Word、网页、数据库），且**检索与生成解耦**——可以独立优化检索组件（比如换更好的 Embedding 模型），而不会影响生成组件的稳定性。

### 1.3 适用场景的风险分级

RAG 并非万能。下表 1-2 按风险等级给出典型用例与 RAG 适用性：

| 风险等级 | 典型案例 | RAG 适用性 |
|---|---|---|
| **低风险** | 翻译、语法检查 | 高可靠性 |
| **中风险** | 合同起草、法律咨询 | 需结合人工审核 |
| **高风险** | 证据分析、签证决策 | 需严格质量控制机制 |

<p><em>表 1-2 RAG 适用场景风险分级</em></p>

在 **高风险** 场景里，RAG 自身不足以担责——它的输出必须配合人工复核、规则校验或领域专家评审。这也是为什么**"可溯源"特性在严肃场景比在闲聊场景重要得多**。

---

## 这是什么

一张"选型二维图 + 风险分级表"的浓缩版：**横轴是 LLM 优化深度、纵轴是上下文优化深度**——Prompt / RAG / 微调 在这张图上有清晰的分界；RAG 不是万能药，也不总是首选药。这一节解决"RAG 是不是适合我手头这个任务"的判断问题。

## 跑起来

```bash
python s00_concepts/units/02_why_rag/code.py
```

本 unit 用 stdlib 算一笔小账：把"长上下文塞进 prompt"和"RAG 检索 + 拼上下文"两种方案，按 token 数 × 单价折算成美元并排打印出来。结论一眼可见——**高频 + 大语料 + 重复问题** 几乎总是 RAG 便宜。

## 它做对了什么

- **零依赖**：纯 stdlib，单文件 5 秒跑完。
- **把"何时用 RAG"量化**：token × 单价 × QPS 三件套变成具体数字，不再凭感觉。
- **留下 s01-s12 的入口**：选型确定后，s04（embedding）、s06（混合检索）、s07（rerank）、s12（部署）是把"RAG 适用" 这四个字落到代码的每一步。

## 它做错了什么（这就是后面章节要解决的）

- **没考虑检索质量成本**——RAG 便宜的前提是"检索召回了对的资料"；召错了一样会幻觉。检索质量的成本在 s06-s07 详谈。
- **没考虑索引维护成本**——"索引热拔插" 听起来美好，但向量库需要持续写入、监控、去重，这部分成本没算进去；s05 / s12 详谈。
- **风险分级表是经验值**——具体业务的风险等级需要法务 / 业务方共同判定；本表只起提示作用。

## 对照 ragflow 怎么做的

RAGFlow 的 [`docs/reference/ragflow-notes/embedding_routing.md`](../../../../docs/reference/ragflow-notes/embedding_routing.md) 体现了"检索与生成解耦"——provider 路由、模型选型、知识库 schema 都是模块化配置，正好对应本节"灵活的可扩展性"。教程 s04 + s05 是这一节的代码版本。

## 思考题

1. 一个 1000 QPS、语料 1 TB 的客服问答系统，Prompt / RAG / 微调 哪个最划算？为什么？
2. RAG 解决了 LLM 的四类局限，对哪一类它其实只是"缓解"而不是"根治"？
3. 索引热拔插听起来美好，但实操里哪些场景会出问题？
